(() => {
  const btns = document.querySelectorAll('button');
  for (const b of btns) {
    const text = b.textContent.trim();
    if (text.includes('RSI') && text.includes('Relative')) {
      b.click();
      return 'clicked RSI: ' + text.slice(0, 50);
    }
  }
  return 'RSI not found';
})()